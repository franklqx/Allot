# Gentler Streak

[awesome-design-md](https://github.com/VoltAgent/awesome-design-md) 的 README **Collection** 里**没有** Gentler Streak；`design-md/` 子目录与 `npx getdesign list` 里也**没有**对应品牌名。

目录里若存在其它品牌，同样多为「README 指向 getdesign」，而不是直接把完整 `DESIGN.md` 放在 Git 里。

**你可以：**

- 到 [getdesign.md/request](https://getdesign.md/request) 申请收录该 App。
- 或自建 `DESIGN.md`，对齐 `PRODUCT_SPEC.md` §13.4（温和、非惩罚性进度）。
